<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;

/**
 * Laravel's stock placeholder unit test.
 *
 * Shipped with the framework's default skeleton to confirm the unit
 * test harness runs. It carries no application behavior and exists
 * only as a scaffolding example.
 */
class ExampleTest extends TestCase
{
    /**
     * A basic test example.
     */
    public function test_that_true_is_true(): void
    {
        $this->assertTrue(true);
    }
}
