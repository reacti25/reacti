name: Flutter CI

# No `paths:` filter. The job below is a required status check; a path
# filter would leave it unreported on backend-only PRs, blocking the
# merge until an admin override. Running it on every PR keeps the check
# always-reporting without that workaround.
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

# pubspec.lock pins Flutter >= 3.41.0 / Dart >= 3.11.0. Use any stable Flutter
# at 3.41 or above. We deliberately don't pin a minor here so future Flutter
# patch releases don't break CI.
env:
  FLUTTER_VERSION: '3.41.x'

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Pub get
        run: flutter pub get

      - name: Format check
        # Fails if any file is not `dart format`-clean; run
        # `dart format .` locally to fix.
        run: dart format --set-exit-if-changed --output=none .

      - name: Static analysis
        run: flutter analyze --no-pub

      - name: Run tests with coverage
        run: flutter test --reporter expanded --coverage

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: app-coverage
          path: app/coverage/lcov.info
          if-no-files-found: warn

  # Android debug build — catches compile/Gradle breaks that the
  # analyze+test job cannot. NOT a required check yet: it is being
  # re-enabled and observed first; promote it to required once it is
  # reliably green.
  build-android:
    name: Build Android (debug)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Pub get
        run: flutter pub get

      # build.gradle.kts pins ndkVersion 29.x; Gradle auto-installs it
      # but only if the SDK licenses are accepted on the runner.
      - name: Accept Android SDK licenses
        run: yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses || true

      - name: Build debug APK
        run: flutter build apk --debug

  # The iOS build stays omitted: flutter_contacts and camera_avfoundation
  # reference iOS-18 APIs the macos-14 runner's Xcode doesn't yet ship.
  # Re-add after a plugin-compatibility pass or a runner-image upgrade.
